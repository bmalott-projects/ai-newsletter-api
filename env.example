# Required (no defaults):
API_PORT=8000
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ai_newsletter
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
OPENAI_API_KEY=your_openai_api_key_here
JWT_SECRET_KEY=your_secret_key_here # Must be at least 32 chars with upper/lower/number/symbol
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Required for tests (no defaults):
TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ai_newsletter_test

# Optional (have default values):
APP_NAME=ai-newsletter-api
ENVIRONMENT=local
LOG_LEVEL=INFO
JWT_ALGORITHM=HS256
