name: ai-newsletter
services:
  api:
    build: .
    profiles: ["api"]
    env_file:
      - .env
    environment:
      # In compose, the DB host is the service name `db`, not localhost.
      DATABASE_URL: postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@db:${DB_PORT}/${DB_NAME}
    ports:
      - "${API_PORT}:${API_PORT}"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      # Dev convenience: live-reload when you run with --reload
      - ./app:/app/app
    command:
      [
        "uvicorn",
        "app.main:app",
        "--host",
        "0.0.0.0",
        "--port",
        "${API_PORT}",
        "--reload",
      ]

  db:
    image: pgvector/pgvector:pg16
    env_file:
      - .env
    ports:
      - "${DB_PORT}:${DB_PORT}"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $DB_USER -d $DB_NAME"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  db_data:
